# Decisions Log

스프린트 중 threshold·포맷·범위를 바꿀 때마다 한 줄씩 기록한다. 회의 없이도 서로 왜 바뀌었는지 알 수 있게 하는 것이 목적.

| 날짜 | 결정 내용 | 이유 | 결정자 |
| --- | --- | --- | --- |
| 2026-07-18 | `timestamp_ms`·`frame_id`를 모든 프레임 기반 메시지의 정식 계약 필드로 확정 | 시선·제스처 temporal alignment(README 9장 Commit 조건 6)에 필수. messages.py 구현과 문서 상태 불일치 해소 | suh1088 |
| 2026-07-18 | Command에 `capability`/`operation`/`value` 포함 (stateless adapter) | adapter가 intent를 재조회하지 않아 구조 단순, dispatch 전 검증도 payload로 수행 가능 | suh1088 |
| 2026-07-18 | 만료 시각 필드명 `expires_at_ms`로 통일 (단위 접미사 규칙: 절대=`_at_ms`, 상대=`_in_ms`) | 문서(`expires_at`)와 코드(`expires_at_ms`) 불일치 해소, 단위 명시 | suh1088 |
| 2026-07-18 | `gesture`·`capability`·`operation`·`target`은 열린 문자열 키(snake_case), 제스처→capability 매핑은 `configs/` 데이터로 관리 | 커스텀 제스처·신규 기기를 코드 수정 없이 추가하기 위함. 닫힌 enum 가정으로 코드 짜면 확장 시 전면 수정 필요 | suh1088 |
| 2026-07-18 | 노트북 Lock 중 커서/제스처 분기: 기본은 커서 모드, Gesture Spotter `ONSET` 감지 시 커서 일시정지 후 제스처 판정 우선. 판정 실패(`IDLE` 복귀) 시 커서 모드 복귀 | 별도 모드 전환 동작 없이 자연스러움. 제스처 시작 순간의 커서 미세 끌림은 감수 | suh1088 |
| 2026-07-18 | 제스처 모델 추론은 MVP에서 로컬 실행. 단, 추론 부분을 교체 가능한 경계로 분리해 나중에 GPU 서버 스트리밍으로 옮길 수 있게 설계 | 데모 당일 네트워크 리스크 제거 + 향후 무거운 모델(ST-GCN 등) 서버 실행 여지 확보 | suh1088 |
| 2026-07-18 | Gaze 방식: 머리+눈 오프셋을 합성한 시선 방향 단위 벡터 + 코사인 유사도 비교 (README 7장) | 등록 시(고개 돌림)와 실사용 시(눈짓만)의 행동 불일치에도 같은 방향이면 같은 벡터가 나오도록 | 팀 합의 |
| 2026-07-18 | command_id를 `cmd-{intent_id}`로 결정적 생성 | 같은 intent 재시도가 같은 command_id로 collapse되어 dedup만으로 idempotency 보장(원칙 3) | suh1088(3인) |
| 2026-07-18 | 상대 연산(increment/decrement)의 결과 절대값 [min,max] clamp는 protocol이 아니라 adapter가 수행 | clamp에 기기 현재 상태가 필요한데 protocol은 상태를 모름. protocol은 delta의 부호·step 배수만 정적 검증 | suh1088(3인) |
| 2026-07-18 | capability 연산 집합(number: set/increment/decrement, boolean: set/toggle)을 capability spec에 선언(config 주입) | 연산을 코드에 하드코딩하지 않고 기기별로 다르게 선언 가능하게. 열린 문자열 키 원칙과 일관 | suh1088(3인) |
| 2026-07-18 | (미해결·논의필요) Intent에 생성 timestamp 없음 → TTL을 "수신 시점 기준"으로 해석. enum capability는 Intent.value(int/float/bool)로 표현 불가라 보류 | 정밀 TTL·enum 기기는 계약 변경 필요. MVP 범위(전구·노트북)에선 문제 없어 후순위 | 3인 제기, Fusion과 협의 대상 |
| 2026-07-18 | capture 소스의 `read()→None`은 "일시적 미스"로만 정의하고, 유한 소스의 스트림 종료는 `EndOfStream` 예외로 분리 | 리뷰에서 발견: 둘을 혼동해 웹캠 프레임 하나 놓치면 파이프라인이 죽던 버그 수정 | suh1088(3인) |
| 2026-07-18 | `CapturePipeline`을 컨텍스트 매니저 + `close()`로 소스 디바이스를 명시 해제. `stop()`은 루프만 중단 | 리뷰에서 발견: `close` 미호출로 `cv2.VideoCapture` 핸들이 프로세스 종료까지 누수 | suh1088(3인) |
| 2026-07-18 | protocol dedup을 `register()` 원자 연산 하나에만 의존하도록 변경(선(先) `seen()` 검사 제거, `DuplicateCommandError`→`Rejected(DUPLICATE)`) | 리뷰에서 발견: 동시 submit 시 검사-등록 사이 경쟁으로 예외가 새어나가던 TOCTOU 수정 | suh1088(3인) |
| 2026-07-18 | `Command`에 `device_id` 필드 추가 (계약 변경) | dispatch 시 어느 adapter(Windows/SmartThings)로 라우팅할지 결정하려면 command이 대상 기기를 알아야 함. Command은 dev-3 경계 안에서만(protocol 생성·adapter 소비) 쓰여 Gaze/Gesture/Fusion 영향 없음 | suh1088(3인) |
| 2026-07-18 | lifecycle에 `VALIDATED→REJECTED` edge 추가. dispatch 시 대상 기기가 registry에 없어 라우팅 불가하면 `REJECTED`(never dispatched)로 처리 | 리뷰에서 발견: 라우팅을 `DISPATCHED` 전이 후에 하던 순서를 바로잡음. 보낸 적 없는 command에 "보냄" 상태를 붙이지 않도록(정직성·telemetry 정확도) | suh1088(3인) |
| 2026-07-18 | `DispatchCoordinator.dispatch()`를 idempotent하게: command이 `VALIDATED`가 아니면 adapter 미접촉·현재 상태 리포트 | 리뷰에서 발견: 재호출 시 `IllegalTransitionError`가 raw로 튀던 것 해소. 중복 실행은 이전에도 방지됐으나 coordinator 레벨에서 "정확히 한 번" 보장을 명시 | suh1088(3인) |
| 2026-07-18 | Python 최소 버전을 3.12로 통일(`requires-python>=3.12`, mypy `python_version=3.12`) | Gaze 병합으로 추가된 numpy 2.5.1 스텁이 PEP 695 `type` 구문(3.12+)을 써서 mypy 3.11 타깃이 numpy 파싱에 실패→타입체크 전체 중단. 팀 전원이 실제로 3.12 venv에서 개발 중이라 3.11 지원 선언만 현실과 어긋나 있었음. 3.11 지원 포기 | suh1088(3인) |
| 2026-07-18 | Gaze UNKNOWN 거부에 최근접 등록 방향과의 최대 각도 25도를 추가 | 등록 기기가 하나면 기기 간 정규화 확률이 방향과 무관하게 1.0이 되어 먼 시선을 선택하는 결함을 방지 | Gaze Targeting 담당 |
| 2026-07-18 | Fusion(`gesture_fusion.alignment.TargetLockTracker`)은 Gaze의 자체 Gaze Lock(README 7장, 커서 게이팅용)과 별개로 자기만의 Target Lock을 `TargetEstimate`(§1) 스트림에서 독립 계산한다 | interface-contract.md §1에는 lock 여부 필드가 없고, 모듈 경계 규칙상 Gaze 내부 구현(`jarvis.gaze.lock`)을 import할 수 없음. 같은 원시 확률·margin 신호를 각자 목적(커서 통과 vs intent commit)으로 독립 판단 — 한쪽 임계값 변경이 다른 쪽에 영향 없음 | 2인(Fusion) |
| 2026-07-18 | Fusion Target Lock의 `target_lock_ttl_ms`는 고정 만료가 아니라 유효한 프레임마다 `timestamp_ms + ttl`로 갱신되는 슬라이딩 윈도우 | 한 대상을 계속 응시하는데 1.5초마다 재-dwell해야 하는 것은 비현실적. 시선이 짧게 흔들려도(유예 기간 안) lock을 유지해 제스처 판정 도중 끊기지 않게 함 | 2인(Fusion) |
| 2026-07-18 | 제스처→capability 매핑을 `configs/gesture_capability_map.json`(신규 stdlib JSON, 새 의존성 추가 없음)으로 관리하고, 매핑 키를 `(target device_id, gesture)` 쌍으로 둔다 | 같은 제스처도 기기에 따라 다른 동작을 뜻함(README 9·15장: 노트북 swipe_down=스크롤, 전구 swipe_down=밝기 감소 — 2026-07-20에 라벨이 slide_two_fingers_down으로 교체됨, 아래 해당 행 참조). gesture만 키로 쓰면 이 구분을 표현할 수 없음. YAML은 새 의존성이라 JSON(표준 라이브러리)을 택함 | 2인(Fusion) |
| 2026-07-18 | (해결됨, dev-3 구현) `gesture_capability_map.json`에 `laptop.swipe_left/right → window_switch` capability를 매핑해 뒀으나, 3인의 `WindowsAdapter`는 아직 `scroll`/`volume`/`media`만 처리하고 `window_switch`는 미구현이었다. → `WindowsAdapter._execute`에 `window_switch` 분기·`_window_switch`(`switch_window(forward, count)`, Alt+Tab hold) 추가로 구현 완료(`test_window_switch_*` 회귀 테스트 포함). macOS 대응 동작은 `switch_window` InputSink(Cmd+Tab)로 pose_control 계층에서 처리 | README 15장 필수 데모 시나리오("노트북 Swipe Left 창 전환")를 완성하려면 dev-3이 핸들러를 추가해야 했음. 지금은 실제 실행됨 | 2인 제기, dev-3 구현 |
| 2026-07-18 | macOS 입력은 ctypes로 CoreGraphics를 직접 바인딩하지 않고 `pyobjc-framework-Quartz`+`pyobjc-framework-Cocoa`(신규 `macos` extra, `sys_platform=='darwin'` 마커)를 채택 | CGEvent의 CFType·구조체 바인딩을 ctypes로 직접 하면 실패 위험이 크고, 미디어 키(NX_KEYTYPE 기반 system-defined 이벤트)는 AppKit `NSEvent`가 필요해 순수 Quartz만으로 불가능. `Win32InputSink`(ctypes)는 전혀 건드리지 않음 — Windows 동작·의존성 불변 | 팀 합의(사용자 지시) |
| 2026-07-19 | 모니터의 `실시간`·`손 추적` 탭은 웹캠을 좌우 반전한 거울상(selfie)으로 **표시만** 한다. 이는 화면 표시 전용이며 MediaPipe 입력·모델 입력·학습 데이터 로깅은 모두 반전하지 않은 원본이다. 구현: `VideoView`가 `cv2.flip`으로 표시용 복사본만 뒤집고(원본 프레임 불변) 오버레이를 `mirror=True`로 그린다. handedness 라벨은 원본 프레임 기준이라 반전하지 않는다(별도 이슈) | 사용자가 손을 자기 손처럼 직관적으로 보도록. 단, 반전을 데이터 경로에 넣으면 학습 데이터가 거울상으로 오염되므로 표시 계층에만 격리 | 모니터 디버그 담당(사용자 지시) |
| 2026-07-19 | `실시간` 탭 웹캠 스켈레톤을 raw 이미지 좌표가 아니라 One-Euro 평활화된 이미지 좌표(`HandSnapshot.image_points_smoothed`)로 그린다. 모델 정규화 공간 평활화(`HandFeatureExtractor`)와 별개인 표시 전용 필터(`HandProbe._image_smoother`)이며 `GestureConfig` 평활 파라미터를 공유한다. `손 추적` 탭의 스무딩 토글(`smoothed`)을 따르고, 끄면 raw로 표시 | 디버그 뷰에서 보이던 정점 떨림이 필터 이전 raw였음. 모델 입력은 이미 평활화돼 있는데 화면만 raw라 실제보다 불안정해 보이는 착시 제거. 데이터·모델 입력에는 영향 없음(표시 전용) | 모니터 디버그 담당(사용자 지시) |
| 2026-07-19 | 손 랜드마크를 (x, y, z) 3D가 아니라 **x·y 2D만** 사용한다(`config.LANDMARK_DIMS=2` 단일 상수가 원시 추출~정규화~feature 전 파이프라인의 좌표 차원을 결정). MediaPipe z(깊이)는 단안 웹캠 추정값이라 노이즈가 크고 특히 손가락이 카메라 쪽으로 접히는 주먹류 동작에서 크게 흔들림. 손목 평행이동(wrist translation) feature도 2D로 따라감(`_WRIST_DIMS=LANDMARK_DIMS`). feature_dimension(default) 144(=위치 42 + 관절각 14 + 속도 42 + 가속도 42 + 손목 속도·가속도 4) | 검출 안정성 향상(참고: reference 프로젝트도 z를 버림). **트레이드오프**: 관절각·palm_scale·속도·가속도·손목 평행이동이 모두 이미지 평면(2D) 기준이 되어, 카메라 축 방향(out-of-plane) 손가락 굽힘/밀기·손 밀기 신호는 표현 불가(swipe 같은 평면 동작은 영향 없음). **주의**: 학습 데이터도 같은 2D로 전처리해야 하므로(모델 재현성) 3으로 되돌리면 재학습 필요 | 모니터/제스처 담당(사용자 지시) |
| 2026-07-19 | (해결됨, 2026-07-19 2인 구현) `normalize_hand`의 손목 원점화가 매 프레임 손목을 `(0,0,0)`으로 만들어, 손 모양·회전이 그대로인 순수 평행이동(swipe)에서는 position·velocity·acceleration이 21개 랜드마크 전부 이론상 0이 됨 — swipe_up/down/left/right를 구분할 유일한 신호(손 전체 이동 방향)가 현재 feature 벡터에 없음. 수정 제안: `HandObservation`에 `wrist_position`(= `raw.points[origin_index] / palm_scale`, 원점화 없이 palm_scale로만 정규화) 필드를 추가하고, `HandFeatureExtractor`가 기존 `landmarks`와 별도로 이를 미분해 `wrist_velocity`/`wrist_acceleration`을 새 `GestureConfig.include_wrist_translation` 그룹으로 추가(기존 4개 그룹 뒤에 이어붙여 순수 추가, 기존 테스트 영향 없음) — 제안대로 구현 완료(아래 33번 행, `feature_dimension` 144차원에 포함) | 학습 파이프라인 설계 중 발견: 데이터·학습으로 고칠 수 없는 구조적 신호 손실이라 학습 시작 전 해결 필요. rotate_clockwise/counter_clockwise는 회전이 보존돼 영향 없음 | 3인 제기, 2인(features.py·landmarks.py 소유자)이 구현 |
| 2026-07-19 | 손가락 관절 위치의 가속도(`GestureConfig.include_acceleration`)를 모델 입력에서 완전히 제거한다(필드·계산·`_assemble` 슬라이스 삭제, config로 끄고 켜는 옵션이 아니라 코드 자체를 삭제). 손목 평행이동 가속도(`wrist_acceleration`, 33번 행에서 추가된 swipe 판별용 별개 신호)는 유지 — 혼동하지 말 것. `feature_dimension(default)` 144→102(=위치42+관절각14+속도42+손목속도가속도4). 모니터(`hand_probe.py`/`app.py`)는 관절 위치 가속도를 표시한 적이 없어(손목 가속도만 벡터 뷰로 노출) UI 변경 없음. `training/`은 `feature_dimension(GestureConfig)`을 동적으로 참조해 코드 변경 없이 자동 반영됨 | 사용자 지시(구체적 사유는 별도로 전달받지 않음) | 사용자 지시 |
| 2026-07-19 | 손목 정지 시에도 `wrist_velocity`/`wrist_acceleration`이 심하게 떨리는 문제의 원인을 `palm_scale` 잡음 증폭으로 진단하고 수정. `wrist_position = origin/palm_scale`은 분자(화면상 절대 위치, ~0.3~0.7)가 일반 landmark 분자(손 안 상대적 차이, 손목 자신은 0)보다 훨씬 커서, 매 프레임 새로 계산되는 `palm_scale`의 잡음이 나눗셈에서 증폭됨(실측: 정지 시 손목 속도 잡음이 손가락 끝 속도 잡음의 약 2.5배). 검토한 대안: (1) 나눗셈 전 origin만 필터링 — palm_scale 자체가 여전히 raw라 오히려 0.15배로 악화, (2) 기존 순서 유지 + palm_scale만 별도 평활화 — 3.85배 감소(0.81→0.21 palm-width/s, 채택). 구현: `HandFeatureExtractor`에 `_palm_scale_smoother`(One-Euro) 추가, `wrist_position × raw_palm_scale / smoothed_palm_scale`로 재조정(`normalize_hand`는 순수 함수라 그대로 유지, origin 재접근 없이 수학적으로 동일 결과 재현). `GestureConfig.smooth_palm_scale`(기본 True)·`palm_scale_smoothing_min_cutoff`/`_beta`(기본 0)/`_d_cutoff`로 제어 | 사용자가 실사용 중 손목 신호 잡음을 직접 보고, 시뮬레이션으로 원인·대안을 정량 검증한 뒤 결정 | 사용자 지시 |
| 2026-07-19 | 모니터 `손 추적` 탭의 손목 이동 **속도**(`_velocity_view`)를 x/y 개별 바(`VectorView`) 대신 방향+크기를 보여주는 화살표(`VectorArrowView` + `overlay.render_vector`, 정규화 손 캔버스 옆 기존 위치 그대로)로 바꾼다. 손목 이동 **가속도**(`_accel_view`)는 요청 범위 밖이라 기존 바 형태 유지 — 두 뷰가 당분간 스타일이 다를 수 있음. 화살표는 running-max 크기로 자동 스케일(정지 시 미세한 떨림과 빠른 swipe 모두 보이게)하고, 웹캠 거울상에 맞춰 x만 반전해 표시(표시 전용, 모델 입력은 원본) | 사용자가 바 형태보다 벡터 시각화가 방향·크기를 더 직관적으로 보여준다고 판단 | 사용자 지시 |
| 2026-07-20 | Windows에서 카메라를 열 때 `cv2.VideoCapture(index)`(기본 MSMF 백엔드) 대신 `cv2.VideoCapture(index, cv2.CAP_DSHOW)` + `cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)`을 쓴다(`OpenCVCameraSource._ensure_open`, `gaze/cli.py:_open_camera` 둘 다). non-Windows는 기존 그대로(백엔드 미지정) | 오픈소스 레퍼런스 프로젝트(`hand-gesture-recognition-using-mediapipe`) 분석 중 발견: MSMF 기본 백엔드는 내부 버퍼링으로 지연이 주기적으로 쌓였다 풀리는 끊김을 유발함(레퍼런스 코드 로컬 패치가 이미 이 방식을 씀). 손 추적이 느리게 따라오는 증상의 카메라 레벨 지연 원인 제거 목적 — `palm_scale` 잡음 수정(36번 행)과는 별개의 지연 소스 | 사용자 지시 |
| 2026-07-20 | 학습 클립 추출 시 "미검출 프레임 하나라도 있으면 클립 전체 제외" 규칙을 `TrainingConfig.max_missing_frame_fraction`(기본 0.3) 기반 허용으로 완화한다(`extract_jester.py`·`record_webcam_clips.py` 둘 다). 허용 범위 안의 미검출 프레임은 캐시에 남기고 `HandFeatureExtractor.push()`가 실시간 추론과 동일하게 reset+0벡터로 처리하며, `training/dataset.py`가 그 프레임의 gesture/phase loss target을 IGNORE_INDEX로 마스킹한다(패딩 프레임과 같은 방식) — 이 마스킹 없이는 0벡터 프레임에 실제 제스처 라벨이 붙어 "신호 없음"을 특정 제스처로 학습하게 되므로 함께 구현해야 안전함. 부수 수정: `augment.py`의 `time_warp`가 리샘플 후 `hand_detected`를 무조건 True로 채우던 하드코딩을 다른 스칼라 필드와 같은 방식(선형보간+0.5 임계)으로 교체(이전엔 클립이 항상 전부 검출된 상태라 문제가 없었지만, 이번 완화로 유지하면 침묵리에 데이터가 오염됨) | 클립당 프레임 수가 많을수록 프레임별 실패율이 지수적으로 증폭돼(1-(1-p)^n, 30프레임 클립은 실패율 5%만 돼도 약 79% 통째 폐기) 학습 데이터 대부분이 버려지는 문제를 사용자가 보고. 학습 파이프라인 점검 중 발견·구현 | 사용자 지시 |
| 2026-07-20 | `training/train.py`의 `train()`에 `stage == "finetune"`인데 `init_from`이 없으면 즉시 `ValueError`를 내는 검사를 추가한다(데이터셋 접근 전, 함수 최상단) | 학습 파이프라인 점검(사용자 지시) 중 발견: 이 검사가 없으면 `--init-from`을 깜빡한 파인튜닝 실행이 무작위 초기화 가중치로 조용히 학습되고도 `gesture_tcn_finetuned.pt`·`ModelMetadata`에 "파인튜닝 결과"로 저장돼, Jester 사전학습을 전혀 거치지 않은 모델을 파인튜닝 결과물로 오인하게 만듦(development-principles.md: 성공을 지어내지 않는다) | 사용자 지시(모델·학습 파이프라인 점검 요청) |
| 2026-07-20 | 손바닥 축(손목→중지 MCP)이 이미지 평면과 이루는 각이 `GestureConfig.max_palm_tilt_degrees`(기본 20°)를 넘으면 자세 판정을 **거부**한다. 각도는 z가 있어야 계산되므로 landmark 소스(`mediapipe_hands`·`hand_probe`)가 `palm_tilt_degrees()`로 계산해 `RawHandLandmarks.palm_tilt_degrees`에 담아 넘기고, `normalize_hand`가 `HandObservation`으로 전파한다 — **좌표는 2D 유지**(LANDMARK_DIMS=2 결정 불변). z를 좌표로 되살리는 것이 아니라, 화면 밖 회전이라는 z만 아는 정보를 각도 하나로 요약해 굵은 임계 판정에만 쓴다. 기울기를 모르는 소스(`None`)에서는 게이트를 걸지 않는다(알 수 없음을 위험으로 간주해 전부 막으면 시스템이 멈춤) | 실측(6클래스, 에피소드 단위 홀드아웃, 183 에피소드): 구간별 정확도 0~10° 90.8%, 10~20° 76.6%, 20~30° 47.3%, 30~90° 37.0%. 기울기 구간만 따로 학습해도 10~20° 39.8%, 20° 초과 25.9%(우연 17%)라 데이터 추가로 해결되는 문제가 아니라 투영에 의한 정보 소실. 정규화 방식 4종(현행 palm_mcp / max-abs / 최대쌍거리 / 중심 RMS) 비교에서 회전 민감도는 30배 차이(CV 0.018~0.332)였으나 정확도는 통계적 동률(81.4~82.7%, 시드 5개)이라 **분모 교체로는 해결 불가** — 현행 palm_mcp 유지. 시간 다수결도 무효(1초 지연에 +0.4%p, 오류가 에피소드 단위로 뭉쳐 최대 37프레임 연속 오답). 임계 20°는 판정률 85.1%/정확도 88.7% 지점으로, 실제 조작에서 손을 화면과 완벽히 나란히 둘 수 없다는 요구(15°는 80.6%/90.0%)와 20° 초과의 급락 사이 타협점 | 사용자 지시(제스처 분류 재시작) |
| 2026-07-20 | 정적 손 자세 분류를 동적 제스처(TCN/swipe)와 **별개 경계**로 신설. `pose_protocol.py`(순수)↔`pose_classifier.py`(torch)는 `model_protocol`↔`model`과 같은 격리 구조. 7-class MLP(index_point/pinch_index/pinch_middle/two_fingers/open_palm/fist/none), 입력 = 정규화 좌표 42 + 손끝 쌍거리 10 = 52차원. 학습은 `training/train_pose.py`(에피소드 단위 홀드아웃), 산출물 `models/hand_pose_classifier.pt`. 전처리(normalize_hand + One-Euro)만 동적 경로와 공유하고 저장 파일의 설정을 로드 시 대조(PreprocessingMismatch) | 손으로 만든 판별자는 핀치/주먹에서 전부 우연 수준(31~34% 오류)이었으나 21점 전체를 보는 MLP는 90%대. 좌표만으로는 손끝 관계를 못 뽑아(index_point 재현율 50.2%) 쌍거리 명시 추가로 92.3%. swipe는 서버 이관 대상이라 로컬 정적 판정과 입력·산출물이 완전히 다름 | 사용자 지시(제스처 분류 재시작) |
| 2026-07-20 | `none` 배경 클래스 추가(제어 자세가 아닌 전이·휴지·일상 동작). 기울기 한계 90°(명령 아님은 기울기와 무관) | 소프트맥스는 반드시 한 클래스를 고르므로 없으면 손이 보이기만 해도 명령이 나감. 우클릭 직전 엄지-중지 접근이 two_fingers로 오분류돼 스크롤 오발동하던 문제 0건, 헐거운 핀치 흡수. 전체 정확도 93.9%→87.1%지만 오류가 안전한 쪽으로 이동(오발동 1.7%, 오인 2.8%, 놓침 15.4%) | 사용자 지시 |
| 2026-07-20 | 정규화 방식은 현행 `palm_scale`(손목→중지 MCP) 유지. **스칼라 기하 지표·게이트 임계**(접촉 거리 등)에는 대신 21점 최대 쌍거리(2D)를 쓴다 | 정규화 4종(palm_mcp/max-abs/최대쌍거리/RMS) 비교: 회전 민감도 30배 차이(CV 0.018~0.332)에도 분류 정확도 통계적 동률(81.4~82.7%, 시드 5개) — 신경망은 일관 스케일이면 분모 무관. 그러나 palm_scale로 나눈 스칼라를 고정 임계와 비교하면 손 기울기에 8.85배까지 흔들려 두 번 오판(회전한 주먹을 라벨 오염으로, 붙은 핀치를 벌어진 것으로). 최대 쌍거리는 가장 덜 단축된 방향을 자동 선택해 z 없이 3D와 동등 | 사용자 지시(재수집 중 발견) |
| 2026-07-21 | 시간축 상태기계 `pose_state.py`(순수) 신설 + 디버깅 툴에서 실제 OS 제어(`pose_control.py`). 커서 이동(index_point·pinch_index, 마우스식 상대 이동+가속, 참조점=손목 평활 이미지 좌표), 클릭/드래그(핀치 유지 시간 분기), 우클릭(pinch_middle), 스크롤(two_fingers가 **가리키는 방향** MCP→끝 벡터, 손 이동 아님), 바탕화면 토글(fist→open_palm 전이, 임시로 F11). dwell 120~300ms 진입, none 3프레임 관용 해제, trusted=False 무시, none이 자세 이력 안 지움. 제어 기본 켜짐, 양 탭 토글 공유 | 프레임별 판정만으로는 동작 미결정(같은 핀치라도 짧게=클릭 유지=드래그). 임계를 전부 시간으로 둠 — 기하학적 임계는 손 각도에 8.85배 흔들렸으나 시간은 투영 무관. 스크롤을 손 이동이 아닌 가리키는 방향으로: 손을 멈춰도 유지되고 이동 추적 불필요(사용자 지시) | 사용자 지시 |
| 2026-07-21 | 실제 OS 제어의 플랫폼 대응·드래그를 마무리. (1) `InputSink`에 `click`/`press(button, down)` 추가, macOS(Quartz CGEvent)·Windows(user32 mouse_event) 양쪽 구현 — F11 외 모든 동작(클릭·우클릭·드래그·스크롤·커서)이 두 OS에서 대응. (2) 드래그 실시간 이동: macOS는 버튼 누른 채 `LeftMouseDragged`를 보내야 창이 실시간으로 따라옴(`move_cursor(dragging=)`), Windows는 `MOUSEEVENTF_MOVE`가 버튼 눌린 채면 OS가 드래그로 해석해 무처리. (3) 주먹→보 전이 키 플랫폼별: macOS=F11(SHOW_DESKTOP, 바탕화면), Windows=PLAY_PAUSE(재생/일시정지). (4) 커서 감도 CURSOR_BASE_GAIN 900→680, 스크롤 스로틀 120→60ms | MouseMoved만 보내면 드래그 대상이 버튼 뗄 때까지 최종 위치로만 튐(사용자 보고). F11은 Windows에서 바탕화면이 아니라 전체화면이라 재생/일시정지로 대체(사용자 지시). 감도·스크롤 속도는 실사용 튜닝(사용자 지시) | 사용자 지시 |
| 2026-07-19 | Gaze target calibration uses look-to-register direction profiles | Demo objects may move and are off-monitor, so target labels are registered by looking at the object. The runtime classifier still follows README section 7: camera-relative gaze `mean_direction + variance`, not screen coordinates or drawn target areas. Unknown rejection remains the default for unregistered or ambiguous directions. | Gaze Targeting owner |
| 2026-07-19 | MVP assumes fixed camera and manual user-change notice | Automatic camera-moved detection and face identity switching are unnecessary for the demo because the camera position is fixed, users will announce profile changes, and moved objects are registered again. | Gaze Targeting owner |
| 2026-07-20 | 물체 등록을 10초·다양한 자세로 늘리고, 머리 이동(parallax)으로 실제 3D 위치+유효 반경을 삼각측량 시도 — 실패하면 2026-07-19의 각도 기반(direction+variance) 등록으로 조용히 대체(`TargetRegistrationSession._try_triangulate`, `calibration/triangulation.py`). 2026-07-19 "look-to-register direction profiles" 결정을 대체하지 않고 확장한다: 이제도 오직 camera-relative geometry만 쓰고 화면 좌표나 그려진 영역은 쓰지 않는다 | 사용자가 "물체의 좌표와 크기"를 직접 예측하길 원함. 단일 모노큘러 웹캠으로는 절대 깊이를 보장할 수 없어(카메라 내부 파라미터 보정 없음), 품질 게이트(baseline·광선 각도 다양성·잔차)를 만족할 때만 채택하고 실패 시 항상 검증된 각도 모드로 폴백 — 지어낸 3D 위치를 반환하지 않는다 | Gaze Targeting owner |
| 2026-07-20 | `radius_mm`은 물체의 실측 크기가 아니라 삼각측량 잔차(RMS)에서 유도한 판정 허용 오차로 정의 | 등록 중 사용자는 물체 중심만 응시하고 경계를 훑지 않으므로 실제 크기를 측정할 방법이 없음 — "크기"를 잔차 기반 허용 오차로 정직하게 재정의(development-principles.md 1절) |  Gaze Targeting owner |
| 2026-07-20 | 3D geometry가 있는 기기도 매 프레임 origin이 없으면(추적 손실 등) 등록 시 저장한 고정 방향(각도 모드)으로 자동 전환 — `origin`은 classify()의 선택 인자이며 기본값 None은 기존 각도 전용 호출부와 완전히 하위 호환 | 프레임 단위 폴백까지 지원해야 3D 도입이 기존 각도 전용 경로(§ 2026-07-19)를 절대 깨지 않는다는 것을 보장할 수 있음 | Gaze Targeting owner |
| 2026-07-20 | MediaPipe facial transformation matrix의 translation(`transform[:3,3]`)을 머리 위치 근사로 채택 — 실측 눈금 검증은 하지 않음(models/README.md) | 카메라 내부 파라미터 보정 없이 구할 수 있는 유일한 깊이 신호. MVP 두 기기(가까운 노트북·먼 전구)는 거리 차이가 커서 근사 오차가 있어도 구분에 충분하다고 판단 | Gaze Targeting owner |
| 2026-07-21 | 실시간 classifier 결과와 3초 연속 응시 후 확정된 target을 별도 신호로 표시하고, Gaze/Fusion 기본 dwell을 모두 3000ms로 통일 | 순간 오분류가 곧바로 사용자의 확정 의도로 보이거나 intent commit으로 이어지지 않게 하며, 디버깅 화면에서 엔진 정확도 문제와 temporal confirmation 문제를 분리해 진단하기 위함 | Gaze Targeting owner |
| 2026-07-21 | 새 target 후보가 3초 dwell을 쌓는 동안 이전 확정 target을 유지하고, dwell 완료 시에만 원자적으로 교체 | 후보가 중간에 취소될 때 확정 선택이 `UNKNOWN`으로 떨어지는 UX를 없애고, 실시간 엔진 결과·교체 후보·확정 결과를 독립적으로 진단하기 위함 | Gaze Targeting owner |
| 2026-07-21 | 개인 gaze 벡터 보정은 geometric vector를 대체하는 절대값 MLP가 아니라 `delta_yaw/delta_pitch`를 예측하는 residual MLP(12→24→12→2)로 구현하고, 새 물체 등록 때 target별 replay 데이터 전체로 재학습한다 | 적은 개인 데이터에서 raw geometry를 안전 기준으로 유지하고 catastrophic forgetting을 막기 위함. held-out 오차가 raw보다 나쁜 모델은 활성화하지 않으며 실시간 경로에는 작은 NumPy forward pass만 둔다 | Gaze Targeting owner |
| 2026-07-21 | 물체 등록을 1단계 중앙점 보정(20초, 자세·거리 변화)과 2단계 경계 확정(16초, 고개 고정·눈으로 네 변 순회)으로 분리한다. 중앙 프레임만 MLP 정답·중심·3D 광선에, 경계 프레임만 각도 영역에 사용하고 target feature profile에는 둘 다 사용한다 | 한 세션에서 테두리를 본 프레임을 중앙점 정답으로 붙이면 MLP가 서로 다른 실제 gaze를 하나의 방향으로 압축하는 label contamination이 발생한다. 3D 삼각측량에도 서로 다른 경계점 광선을 섞으면 한 점으로 수렴하지 않는다 | Gaze Targeting owner |
| 2026-07-21 | Gaze 확정 target은 순간 UNKNOWN에는 유지하지만 classifier 결과가 2000ms 연속 UNKNOWN이면 해제한다. 알려진 target 프레임이 들어오면 타이머를 초기화하며 Gesture/Fusion은 변경하지 않는다 | 후보 취소 때마다 즉시 UNKNOWN으로 떨어지는 깜빡임은 막되, 사용자가 등록 물체를 실제로 2초 이상 보지 않을 때 과거 선택이 무기한 남는 문제를 막기 위함 | Gaze Targeting owner |
| 2026-07-21 | 개인 target classifier의 표준화 feature 우선도를 gaze yaw/pitch `2.0`, head yaw/pitch/roll `0.4`, face scale `0.6`으로 두고, 학습 후 class별 head 유효 가중치를 gaze의 20% 이하로 제한하며 저장 샘플을 새 정책으로 재학습한다. 프레임 차분은 시간 기반 gaze 속도·가속도로 확장하고 target 중심 방향 보너스(최대 `+0.35`, `+0.15`)를 Linear classifier와 area matcher에 공통 적용한다. blink/hold/iris jump 중 history는 갱신하지 않으며 Gesture/Fusion은 변경하지 않는다 | feature scaling만 하면 모델이 더 큰 head coefficient를 학습해 우선도를 상쇄할 수 있다. 기존 개인 classifier가 고개 방향으로 오분류되는 문제를 확실히 줄이고, 겹친 target에서는 실제 시선 이동 방향을 tie-break evidence로 사용하기 위함 | Gaze Targeting owner |
| 2026-07-21 | 위의 residual MLP·Ridge·개인 Linear-softmax 결정을 폐기하고, 새 등록을 8D 통계 profile(`[gaze yaw/pitch, head yaw/pitch/roll, face scale, face center x/y]`)과 정밀 테두리 area로 통일한다. area 밖은 항상 UNKNOWN이며 area 안 후보만 Mahalanobis distance로 검증·정렬한다 | 적은 사용자 데이터로 target ID를 학습한 모델이 과거/삭제 target class와 편향을 유지하고, 사용자가 조정할 수 없는 오분류를 만들었다. 등록 데이터의 평균·공분산은 현재 물체만 표현하고 각 입력의 진단값을 직접 확인할 수 있다 | Gaze Targeting owner |
| 2026-07-21 | 두 등록 단계 모두 테두리를 훑는다. 1단계는 자세·거리·화면 내 얼굴 위치를 바꾸고, 2단계는 고개를 고정해 정밀 영역을 만든다. 서로 다른 표면점의 ray를 한 점으로 삼각측량하지 않으며 새 `position_3d`를 생성하지 않는다 | 경계 ray의 교차점은 물체 중심이 아니므로 3D 좌표를 만들면 수학적으로 잘못된 결과가 된다. 단안 데모에서는 face scale/location 문맥이 사용자의 위치 변화 보정에 더 직접적이고 진단 가능하다 | Gaze Targeting owner |
| 2026-07-21 | 겹친 area의 tie-break는 실시간 가속도 대신 홍채 이동이 `12 deg/s` 이상으로 시작해 `4 deg/s` 아래로 감속할 때의 마지막 이동 방향을 500ms 기억해 사용한다. 양안 blink는 눈별 baseline을 두고 양쪽이 함께 닫힐 때만 hold하며 smoothing deadzone은 5°에서 1°로 낮춘다 | 감속 가속도는 이동 방향의 반대를 가리킬 수 있고, 한쪽 눈의 원근 축소를 blink로 오판하면 head turn 중 화살표가 멈춘다. 마지막 이동 속도와 양안 조건이 사용자가 실제로 시선을 놓은 방향을 더 안정적으로 나타낸다 | Gaze Targeting owner |
| 2026-07-21 | 테두리 area 안의 target은 8D Mahalanobis 문맥이 멀어도 hard reject하지 않는다. gaze area를 eligibility로 유지하고 head pose·face scale·face center는 겹친 후보의 soft ranking에만 사용한다 | 같은 물체를 볼 때 눈이 고개 회전을 상쇄하면 최종 gaze는 같아도 head yaw가 등록 평균에서 크게 달라진다. head context가 이 경우를 UNKNOWN으로 만들면 gaze targeting 목적과 충돌한다 | Gaze Targeting owner |
| 2026-07-21 | `TRACKING LOST`는 실제 `face_detected=False`에만 사용한다. 얼굴은 있지만 blink/recovery로 홍채를 못 쓰면 마지막 실측 gaze 기준 300ms만 hold하고 이후 `head-only` confidence 0.45로 전환한다. held frame이 hold 기준시각을 갱신하지 않게 한다 | 실카메라 150프레임에서 얼굴 검출은 150/150이었는데도 얼굴 손실 배너가 나타나, UI가 iris/gaze 초기화 실패를 얼굴 검출 실패로 잘못 표시함을 확인했다. hold timestamp를 매 프레임 갱신하면 무한정 오래된 gaze가 남는 문제도 함께 방지한다 | Gaze Targeting owner |
| 2026-07-21 | 2단계 테두리 yaw/pitch 샘플을 중심+반경 타원으로만 압축하지 않고 95퍼센타일 outlier 제거 후 convex hull 꼭짓점으로 저장한다. 중심→현재 gaze ray가 hull과 만나는 경계를 정규화 거리 1.0으로 사용하고, polygon 없는 과거 JSON만 타원 fallback을 유지한다 | 현재 gaze가 yaw·pitch 각 축 범위에는 모두 들어와도 타원식 `sqrt(dx²+dy²)` 때문에 사각 물체 모서리가 UNKNOWN이 되는 실제 사례(`0.98`, `0.88` → `1.32 OUT`)를 확인했다. 사용자가 훑은 경계 내부 전체를 보존해야 한다 | Gaze Targeting owner |
| 2026-07-22 | 등록 1단계 샘플에서 target별 head-yaw 구간 gaze 보정(`TargetPoseCorrection`)을 학습해 area 판정 직전에 적용한다. 2단계 head yaw를 기준 자세로 보정을 0으로 정규화하고, 표본 부족 bin과 유효 bin 2개 미만은 저장하지 않으며 오프셋은 ±10°로 제한한다. 전역 `head_yaw_weight` 조정과 근안 우선 가중은 채택하지 않는다 | `diagnose-composition` 실측(494프레임, head yaw -38~+45°)에서 iris 추정이 \|head yaw\| 15° 밖 좌우 모두 포화·역행함을 확인했다(중앙 ±10° R²=0.90/implied 0.43, 그 밖 기울기 부호 반전, 양안 모두 무신호). 전역 가중치는 중앙을 고치면 극단을 악화시키는 구조라 불가능하고, 포화 편향은 자세별로만 반복 가능하므로 target별 자세 조건부 보정이 유일한 결정론적 경로다 | Gaze Targeting owner |
| 2026-07-22 | 등록 1단계를 "테두리 훑기 + 자세 변화"에서 "중앙 한 점 응시 + 고개 스윕"으로 바꾸고, `build_pose_correction`에 bin 내 gaze IQR 게이트(기본 4°)를 추가한다 | 테두리를 훑으며 고개를 돌린 1단계 데이터로 pose 보정을 배우면 사람이 보는 방향으로 고개를 돌리는 상관관계 때문에 bin 중앙값이 "그 자세에서 보던 테두리 위치"를 학습해 편향의 부호가 뒤집혔다(실측: head yaw -25~-30 실제 편향 -3.4° vs 학습값 0~+3.9°, 재등록 후에도 실사용 OUT). 중앙 응시 스윕은 같은 구간에서 IQR 1~2°로 반복 가능했고, \|head yaw\| 30° 밖은 IQR 9~30°라 IQR 게이트로 걸러낸다 | Gaze Targeting owner |
| 2026-07-22 | pose 보정을 rescue 전용으로 바꾼다: 원시 gaze와 보정 gaze 중 area 거리가 작은 쪽을 채택(min)하고, 보정이 원시 IN 프레임을 OUT으로 만들 수 없게 한다. \|head yaw\| 30°+ / head pitch 25°+ 자세는 보정 대상이 아니라 데모 배치로 회피한다 | 중앙 응시로 재학습한 뒤에도 자세별 편향이 세션·순간에 따라 -8°~+4°로 요동해(응시 캡처끼리는 일치, 정지 응시 실사용과는 부호 반대), 양방향 보정이 원시로는 IN이던 실사용 프레임 2건을 OUT으로 밀어내는 회귀를 만들었다. 변동폭이 area 반경과 같은 규모면 고정 오프셋은 이득 기대값이 0 이하이므로 해를 끼칠 수 없는 단방향 적용만 남긴다 | Gaze Targeting owner |
| 2026-07-22 | dwell 적립(CANDIDATE) 중 순간 UNKNOWN/저신뢰는 `candidate_grace_ms`(600ms)까지 유예하고, 그 이상 지속될 때만 SEARCHING으로 리셋한다. 유예 구간도 dwell 경과에 포함된다 | 깜빡임 한 번의 UNKNOWN 프레임으로 3초 dwell이 0으로 리셋되면 자연 깜빡임 주기(2~5초)보다 dwell이 길어 확정이 영원히 되지 않는 실사용 증상("몇 초 간격으로 다시 시작")을 확인했다. blink hold(300ms)+recovery(250ms)가 못 덮는 꼬리를 상태 머신에서 흡수한다 | Gaze Targeting owner |
| 2026-07-22 | 등록 1단계를 시간제(20초)에서 조건 충족식으로 바꾼다: 정면/좌/우/상/하/근/원 7개 coverage 구간이 각 30프레임을 채워야 2단계로 넘어가고, 미달이면 finalize를 거부하며 상태줄이 부족 구간을 실시간 안내한다. 완료 시 1단계 원시 샘플을 JSON으로 저장한다 | 시간제 등록은 스윕이 한쪽에 치우쳐도 완료돼, 실제로 정면·왼쪽 보정점 없이 오른쪽 bin 2개만 저장된 등록이 발생했다. 원시 샘플 보존은 재캡처 없이 오프라인 보정 실험(A/B)을 가능하게 한다 | Gaze Targeting owner |
| 2026-07-22 | Ridge residual 보정은 오프라인 A/B 도구(`jarvis-gaze ab-residual`, leave-one-yaw-bin-out)로만 두고 런타임에 연결하지 않는다. 입력에서 raw gaze 2차원을 제외하고 자세·문맥 6D로만 delta를 예측한다 | 단일 target 데이터에서 gaze를 입력에 넣으면 정답이 delta=center-gaze라 모델이 항상 물체 중심만 출력하는 상수 예측기로 붕괴하며 leave-bin-out으로도 잡히지 않음을 단위 테스트로 확인했다. residual 회귀는 2026-07-21에 제거된 이력이 있고 자세별 편향이 세션 간 요동하므로, 같은 캡처 내 A/B 통과만으로는 활성화하지 않는다 | Gaze Targeting owner |
| 2026-07-21 | 커서 감도를 실측 기반으로 재튜닝: `CURSOR_BASE_GAIN` 680→480, y축 감도 0.5배(`CURSOR_Y_GAIN_SCALE`), `CURSOR_DEADZONE` 0.006→0.007, `CURSOR_MAX_ACCEL` 3.5→4.0(accel_gain 1.4 유지), 데드존을 하드컷에서 **soft deadzone**(경계에서 이동량 0부터 연속)으로, 커서 speed 분모 `palm_scale`에 One-Euro 평활 추가 | 임시 실측 툴(`.claude/tmp/`)로 실사용 손 속도 분포(이봉: 저속 정밀+고속 스와이프)와 정지 잡음(p95 0.13 팜폭/초)을 재고 결정. base_gain이 미세조정을 지배해 낮추고, 정지 잡음은 손목 좌표 지터라 데드존은 낮추지 못하고 소폭 상향. palm 평활은 정지 잡음엔 무효였으나(A/B 실측) 이동 중 대비로 유지 | 사용자 지시 |
| 2026-07-21 | 빠른 두 번 핀치 더블클릭 판정을 두 클릭 _확정(_leave)_ 간격이 아니라 두 핀치 **진입**(`_state_since`) 간격으로 바꾸고 `DOUBLE_CLICK_MS` 400→600 | 확정 간격엔 dwell(진입)·release(이탈) 히스테리시스 지연이 포함돼 체감 간격과 어긋남. 시뮬레이션 결과 성립 창이 hold≈200×gap 100~200의 좁은 사각형뿐이었고, 진입 기준+600ms로 gap 100~400까지 확대 | 사용자 지시 |
| 2026-07-21 | 자세 dwell을 핀치 클릭류(pinch_index/pinch_middle) 60ms, 그 외(index_point/two_fingers/open_palm/fist) 기본 120ms로 통일. `TRANSITION_WINDOW_MS`(fist→open_palm 전이 인정) 800→1000ms | 반응성 실험. 핀치 클릭은 빠른 반응 우선, 나머지는 120으로 단축(two_fingers 스크롤 진입도 300→120). 전이는 주먹 풀고 손 펴는 시간 여유 확보 | 사용자 지시 |
| 2026-07-21 | 스크롤: 이동 속도 2배(`SCROLL_TICKS` 1→2), two_fingers 기울기 제한 제거(40°→90° 무제한), 손가락 **폄 게이트**(`MIN_FINGER_EXTENSION`) 추가 | two_fingers는 30~90°에서도 99% 정확해 tilt 게이트가 오히려 아래를 가리키는 정상 스크롤을 막았음. 멈추려 주먹 쥘 때 손끝이 아래로 스윙해 역방향 스크롤이 튀는 문제를, 검지·중지 폄 정도(palm_scale 정규화 MCP→끝 거리)가 임계 미만이면 스크롤을 끊어 해결(디버깅 툴에 폄 값 실시간 표시) | 사용자 지시 |
| 2026-07-21 | macOS 손 펴기 동작을 F11(바탕화면)→Mission Control(`open -a`로 앱 직접 실행), Windows는 Task View(Win+Tab). 손 검출 상한 4→2. 제스처 모델 confidence·엔트로피를 [0,1]로 clamp | Ctrl+↑·F3 키 합성은 시스템 단축키 설정에 의존해 안 열리는 환경이 있어 앱 직접 실행으로. Windows Task View는 macOS Mission Control 대응. float32 softmax 합이 1.0을 ~1e-7 초과해 `ModelPrediction` 검증에 걸려 크래시하던 버그 수정 | 사용자 지시 |
| 2026-07-21 | 정적 포즈에 `middle_point`(중지 하나) 추가 → 8-class(순서: index_point 바로 다음). 동작 = **탭 닫기**(진입 1회, dwell 500ms — 최초 250ms에서 상향). 어댑터에 `InputKey.CLOSE_TAB` 신설, macOS Cmd+W(Quartz CGEvent+Command 플래그)·Windows Ctrl+W(keybd_event 조합)로 양 플랫폼 대응. 수집기(`pose_collect.py`) 라벨·키(1~7)·목표(자세 1500/none 4500) 갱신. 학습기(`train_pose.py`)는 label_names를 npz에서 읽는 데이터 주도라 코드 변경 없이 자동 확장 | 사용자 지시(중지 포즈로 탭 닫기, 검지와 일관된 이름, mac/win 동일 동작). dwell을 기본(120)보다 길게: 탭 닫기는 되돌리기 어려워 전환 중 스치는 중지에 오발동 방지. 인식은 재수집·재학습 후 유효(당시 모델은 7-class; 2026-07-22 8-class v2로 교체·승격) | 사용자 지시 |
| 2026-07-22 | 검지 회전 → 볼륨 조절 신설(`_track_rotation`). 검지 MCP→TIP 각도를 누적, `ROT_STEP_DEG`(60°)마다 volume_up/down 1스텝. `ROT_MIN_SPEED`(60°/s) 미만 무시, `ROT_SIGN`(-1, 실기기 확인)으로 방향. **게이트는 확정 상태가 아니라 분류기 라벨** — 낮은 tilt 진입 없이 손 눕힌 채 바로 회전 시작 가능(고tilt에선 상태 진입 못 하나 라벨 88% 유지). 회전 감지 시 `ROT_HOLD_MS`(400ms) **볼륨 노브 모드**로 다른 모든 동작 차단. pose_control에 volume_up/down 실행(양 플랫폼 VOLUME_UP/DOWN) | 측정(rotation_probe, 1235프레임): 회전 중 index_point 라벨 88%·tilt 중앙 16°(>10°라 커서 자연 정지)·각속도 중앙 ~400°/s. 사용자 피드백 반영: ①방향 반대 → ROT_SIGN 반전 ②고tilt 시작 원함 → 상태가 아닌 라벨 게이트 ③회전 중 오인식 클릭/드래그 → 노브 모드로 전면 차단. 파라미터는 실측 기반·실기기 튜닝 대상 | 사용자 지시 |
| 2026-07-22 | 규칙 2 수정 + `index_point` tilt 20→10°. 기존엔 `trusted=False`(기울기 초과)가 상태를 안 끊고 흘려보내 **이미 진행 중인 커서 이동이 각도를 넘겨도 계속**됐다. 이제 관용(`UNTRUSTED_GRACE_FRAMES`=3, 순간 튐 흡수) 초과로 지속되면 **연속 동작(커서·스크롤)을 정지**하되 상태·진입 dwell은 유지 → 각도를 다시 낮추면 **dwell 재대기 없이 즉시 재개**. 정지 시 커서 참조점을 버려 재개 급점프 방지 | 사용자 관찰(검지로 커서 이동 중 손을 눕혀도 이동이 안 멈춤). tilt 게이트가 진입만 막고 진행 중 동작엔 무력했음. 상태 유지는 회전 등 고기울기 조작으로의 확장을 위한 것이기도(각도 높을 때 커서는 멈추되 상태는 살아있어야 그 위에 다른 동작을 얹음). index_point 10°는 커서가 정면 조작 위주라 좁혀도 무방 | 사용자 지시 |
| 2026-07-22 | 정적 포즈 데이터 처음부터 재수집(2세션·1인 → 클래스당 ~48 에피소드, 각도 다양성 확보) 후 **8-class v2 학습·승격**(`models/hand_pose_classifier.pt`, 전체 94.4%, middle_point 92.5%). 이전 7-class(87.1%) 가중치는 저장소에 보존하지 않음. `DEFAULT_POSE_TILT_LIMITS`에 **`middle_point: 30.0`** 명시 추가 | 이전 pinch_middle 68% 정체는 불균형이 아니라 각도/에피소드 다양성 부족이 원인(밸런싱 실험은 87→88% 소폭에 그침, 재수집으로 68→99.4% 해소). middle_point는 tilt 엔트리 부재 시 폴백 20°가 진짜의 56%를 조용히 거부 → 30°가 진짜 84% 유지·`none→middle_point` 오분류 71% 차단으로 최적(진짜 중앙 21° vs 오분류 37°). 손가락 펴짐 휴리스틱은 진짜·leak 분포가 겹쳐 무력, 기울기·dwell만 유효 | 사용자 지시 |
| 2026-07-22 | 비선형 residual 후보의 런타임 채택 관문을 교차 세션 평가로 코드화한다: `ab-residual --train/--eval`이 세션 A로 학습해 세션 B에서만 평가하고 raw와 bin 보정표를 모두 이긴 bin 과반 시 PASS, 서로 다른 날 eval 2세션 PASS가 활성화 필요조건이다. 1순위 후보로 가우시안 커널 국소 회귀를 구현하되 런타임 미연결로 두고, `verify-target --save-samples`로 등록 없이 세션 데이터를 축적한다 | 같은 캡처 안의 split은 자세별 편향의 세션 간 요동(-8~+4° 실측)을 볼 수 없어 통과해도 실사용에서 어긋났다. 전역 선형(Ridge)은 λ와 무관하게 정면 오차를 2배로 만들며 기각됐고, 커널 국소 회귀는 bin 보정표(boxcar 커널)의 연속 일반화라 정면-희생 구조 문제가 없으며 합성 비선형 편향 테스트에서 Ridge·yaw 단독 표를 모두 이겼다 | Gaze Targeting owner |
| 2026-07-22 | Gaze 확정 dwell을 3000ms에서 1500ms로 줄이고, 확정 해제 UNKNOWN 타임아웃을 2000ms에서 3000ms로 늘린다(빨리 잡히고 천천히 풀리는 비대칭). Fusion의 target_dwell_ms(3000)는 별도 모듈 소유라 변경하지 않음 — 통일이 필요하면 Fusion 담당과 협의 | 실사용에서 3초 확정이 데모 체감상 느렸다. 짧아진 dwell의 오탐 승격은 확률 0.80+마진 0.20 게이트와 깜빡임 유예 600ms가 그대로 막는다 | 사용자 지시 |
| 2026-07-22 | 카메라 정면 근처(예: 노트북)에 놓인 target으로 "돌아올 때"만 고개 끄덕임(dip→recovery) 확인을 요구하는 게이트를 Gaze Lock에 추가한다. 최초 확정이나 같은 target을 계속 보던 중에는 걸리지 않고, 실패해도 후보를 리셋하지 않고 승격만 보류한다(`nod_gate_pending`) | 데모 배치상 카메라를 노트북 근처에 둘 수밖에 없는데(정확도 범위 |head yaw|≤25° 확보), 그러면 노트북 방향이 "가만히 있을 때의 기본 시선 방향"과 겹쳐 다른 target(전구)을 안 보고 있을 뿐인 순간에도 노트북으로 오확정될 위험이 있다. 전구는 각도가 확실히 갈라져 있어 이 문제가 없으므로 게이트를 대칭 적용하지 않는다 | 사용자 지시 |
| 2026-07-22 | **Philips WiZ 전구 adapter 신설**(`adapters/wiz.py`) — 클라우드/토큰 없이 로컬 UDP(38899, `setPilot`/`getPilot`)로 직접 제어. `room.bulb`의 실행 백엔드로 `jarvis.runtime.devices` 레지스트리에 배선. SmartThings와 같은 `DeviceAdapter` 계약·정직성 규약(readback VERIFIED, 실패 UNVERIFIED, 미설정 UNCONFIGURED). 주소는 `IP`/`MAC`/`MAC@IP`(권장) | 데모 당일 SmartThings 전구 UUID·클라우드 왕복 리스크 회피. LAN 한 홉 왕복 15~47ms로 제스처 제어에 적합. `MAC@IP`는 평소 IP 직사(탐색 0)·무응답 시 MAC 재탐색으로 DHCP 변경 흡수(AP 클라이언트 격리 네트워크 대비) | dev-3(사용자 지시) |
| 2026-07-22 | 전구 회전 매핑을 **색온도(color_temperature)→색상(color, hue 0~360°)**으로 변경. `color`는 순환량이라 WizAdapter가 유일하게 클램프하지 않고 360°에서 감아 돈다. `color_temperature` capability는 프로필에 남기되 어떤 제스처에도 매핑하지 않음 | 색온도 전 구간(2700~6500K)이 '흰색의 미묘한 차이'라 무대에서 변화가 안 읽혔다. 색상은 60°마다 색 이름이 바뀌어(빨강→노랑→초록→…) 6번이면 한 바퀴. 클램프하면 회전 제스처가 양 끝에서 죽어 순환 유지 | dev-3(사용자 지시) |
| 2026-07-22 | 시연 가시성 튜닝: 전구 brightness 델타 10→30(범위 10~100의 1/3, step 10 배수라 검증 통과), 회전 color 60°/스텝. slide_left/right를 전구 밝기(±30)에 **이중 매핑**(up/down과 동일) — 상하 인식이 덜 될 때 좌우로도 밝기 조절. 상대 연산은 델타만 검증되고 결과 클램프는 adapter 몫이라 경계에서 멈춤 | 이전 델타는 한 제스처당 변화가 무대에서 안 보였음. 상하 슬라이드 인식률이 낮은 상황 대비 이중 경로 | dev-3(사용자 지시) |
| 2026-07-22 | 모니터 **`시연` 탭 신설**: 비어 있던 "실시간 `TargetEstimate`·`GestureEstimate` 스트림 → `FusionEngine`" 배선을 `demo_bridge`(Qt 무관 순수 코어)로 채워 시선→제스처→Fusion→실기기 전 경로 완성. ①물체 등록 id(`target_001`)→기기 id(`room.bulb`) 치환(미매핑은 UNKNOWN 처리) ②gaze lock 실패 대비 **타깃 고정 폴백**(합성 TargetEstimate, `--no-gaze`서도 동작) ③임계값 프리셋(`reconfigure()`가 frozen config째 FusionEngine 재생성). 기기 실행은 `ExecuteWorker`(별도 스레드), 화면 확인용 `virtual_bulb`(명령 기준 시각화, 실물 응답 아님 — 배지로 분리) | composition root는 CommitDecision 이후만 묶여 있었고 그 앞단 실시간 배선이 없었음. WiZ는 동기 UDP라 무응답 시 타임아웃(3초)만큼 GUI가 얼어붙어 판정(동기)/실행(비동기) 분리 필요. 정직성: 실물 실패 시 가상 전구가 밝아지면 성공 위조라 실물 결과 배지와 분리 표시 | 공동(사용자 지시) |
| 2026-07-22 | 시연 느슨 프리셋의 제스처 확신도 하한 0.55→0.35 | 현장 조명·자세에서 제스처 confidence가 0.55에 자주 못 미쳐 커밋이 죽었음. 느슨 프리셋은 데모 시연용이라 오발동보다 놓침 방지 우선 | 공동(사용자 지시) |
| 2026-07-22 | 등록 1단계 coverage에서 좌/우, 상/하를 개별 필수에서 짝 중 한쪽만 요구하도록 완화하고 `coverage_yaw_side_threshold_deg`를 20도에서 15도로 낮춘다 | 실사용 2차 실측: 물체가 카메라 기준 오른쪽에 있으면 반대쪽("고개 왼쪽")을 채우려면 눈이 반대편으로 다시 꺾여야 해 보상각이 40도 안팎까지 벌어져 "너무 어렵다"는 피드백을 받았다(오른쪽 234/30 vs 왼쪽 0/30). 양쪽을 개별 필수로 두면 물체 위치에 따라 구조적으로 끝나지 않는 등록이 생긴다 | 사용자 지시 |
| 2026-07-22 | 노트북 slide_left/right를 **창 전환(window_switch, Alt+Tab/Cmd+Tab)→가상 데스크톱 전환(desktop_switch)**으로 변경. 어댑터가 Windows는 Ctrl+Win+←/→, macOS는 Ctrl+←/→(Space 이동)를 합성. capability 이름도 `window_switch`→`desktop_switch`로 개명(config·devices·adapter·테스트 일괄) | 좌우 슬라이드는 '옆 데스크톱으로 넘긴다'는 공간적 은유와 더 맞다(창 스위처는 순환 목록이라 좌우 방향성이 약함). 동작이 바뀌었는데 이름을 두면 misnomer라 함께 개명 — no silent caps | 사용자 지시 |
| 2026-07-22 | 시연 탭은 gaze/HUD를 숨기되 **MediaPipe 손 스켈레톤은 표시**한다. 기기 등록은 원본 2단계 캘리브레이션이 있는 `시선 인식` 탭에서만 수행하고 시연 탭은 바로가기 버튼을 제공한다. 등록 `device_type`은 `computer→laptop`, `electric bulb→room.bulb`로 자동 연결하되 사용자의 명시적 `연결 안 함`을 유지한다. `기기 명령 실행`은 TCN→Fusion→Intent의 단일 실제 실행 게이트이며, 시연 탭에서는 정적 pose OS 제어를 막아 노트북 명령의 이중 실행을 방지한다 | 관객 화면을 비운 뒤 손 검출 여부까지 사라져 제스처 입력을 진단할 수 없었고, 등록 id와 런타임 id의 수동 매핑 누락이 모든 target을 UNKNOWN으로 만들었다. 또한 노트북 대상에서 pose scroll과 TCN scroll이 동시에 실행될 수 있어 판정/실행 분리와 exactly-once 시연 의미를 깨뜨렸다 | 사용자 지시 |
| 2026-07-22 | `two_fingers` 스크롤 방향의 최소 수직성 `MIN_VERTICALITY`를 0.5(수직에서 60°까지 허용)에서 cos(30°)≈0.866(수직에서 30°까지만 허용)로 올린다 | 손가락을 옆으로 크게 기울여도 세로 스크롤로 인정되는 범위가 너무 넓었다 | 사용자 지시 |
| 2026-07-22 | 시연 탭 기본값을 **실행 켜짐 + 타깃 고정 laptop**으로 변경(`기기 명령 실행`·`타깃 고정` 체크박스 기본 켜짐, 고정 대상 laptop). 패널 초기 상태를 `_build_demo_tab`에서 브릿지에 명시 동기화(생성 중 시그널 미발화 회피) | 정적 포즈는 게이트 없이 바로 되지만 동적 제스처는 시연 배선(실행 토글+대상 lock)을 타 매번 두 토글을 켜야 했다. 시연 시작 즉시 동적 제스처로 노트북 제어가 되게 함. 전구 시연 때는 타깃 고정을 끄거나 room.bulb로 바꾼다 | 사용자 지시 |
| 2026-07-22 | macOS `desktop_switch`를 **osascript(System Events) Ctrl+화살표 키 코드**로 구현. SkyLight·CGEvent 합성 경로는 폐기 | 실측: 프로세스가 직접 만든 CGEvent 합성 키(HID·session·annotated tap·flag-only·명시적 소스 전부)는 커서·물리 키는 되는데도 Space 전환만 WindowServer가 무시했다. SkyLight 직접 전환(`CGSManagedDisplaySetCurrentSpace`)은 되지만 창↔Space 소속이 꼬여 미션 컨트롤 아티팩트가 생겼다. Apple 자체 프로세스 System Events가 보낸 키만 진짜 단축키로 인정돼 네이티브 그대로(애니메이션·창 관리 정상) 전환됐다. 자동화 권한 필요(첫 호출 프롬프트). 끝 데스크톱에서 새로 만들지 않고 무동작(native 동일) | 사용자 지시 |
| 2026-07-22 | 데스크톱 전환 방향을 제스처와 **반대**로 매핑(오른쪽 슬라이드=왼쪽 데스크톱). macOS·Windows 어댑터 양쪽 동일 | 사용자가 실사용에서 방향이 반대로 느껴진다고 판단 | 사용자 지시 |
| 2026-07-22 | 등록 시 테두리 영역 반경 상한(`registration_max_area_radius_deg`)을 6.0도에서 4.5도로 낮춘다 | 모니터처럼 카메라에 가깝고 큰 물체는 테두리 트레이싱이 상한에 그대로 붙어(구 등록값 6.0/5.63도) 영역이 필요 이상으로 넓게 저장됐다. 근처 target(전구, 중심각 29도 차이)과 실제로 겹치진 않지만 사용자가 여유가 과하다고 판단해 상한 자체를 낮춤 — 기존 등록 데이터는 재등록해야 새 상한이 반영된다 | 사용자 지시 |
| 2026-07-22 | 노트북 가상 데스크톱 전환을 **정적 `two_fingers` 방향 전이**로 인식하는 경로 신설(동적 slide 제스처 대체 실험). 두 손가락이 가리키는 수평 방향의 부호가 한쪽→반대쪽으로 넘어가면 즉시 스와이프 1회(`pose_state._track_swipe` → `PoseEvent("desktop_prev/next")` → `pose_control.switch_desktop`). 전이 창·재무장 dwell·2단 문턱 없이 부호 뒤집힘만으로 발화하고, 빠른 스윙 중 포즈가 잠깐 끊겨도 커밋 방향을 유지(`_leave`에서 side 미초기화)해 미발화를 막는다. 위/아래(스크롤)와 좌/우(스와이프)를 가르는 기준은 `MIN_VERTICALITY` 하나로 통일 | 실측(2026-07-22 two_finger_direction_probe 413샘플): 옆으로 향한 two_fingers는 98~100% 신뢰 인식, 좌/우는 dx 부호로 완전 분리(범위 무겹침). 단 정적 '각도'만으론 스크롤과 12% 겹쳐 단일 각도 게이트로는 분리 불가 → 각도가 아닌 **부호 전이**로 감지. 라이브 실측에서 좌우 대칭 발화 확인(prev 8/next 8) | 사용자 지시 |
| 2026-07-22 | `MIN_VERTICALITY`를 cos(30°)에서 **cos(20°)≈0.940**으로 좁힌다(수직으로 보는 범위 축소). 이 상수는 이제 스크롤(위/아래)과 스와이프(좌/우)를 가르는 **공용 기준**이다 | 실측상 '왼쪽' 두 손가락 포즈가 사선(수평에서 μ54°, |dy|≈0.81)으로 들려 cos30° 경계를 자주 넘어 수직으로 오분류됐고, 그 결과 오른쪽 스와이프의 출발점인 '왼쪽 커밋'이 안 잡혀 오른쪽 전환이 씹혔다. 20°로 좁히면 사선 왼쪽도 좌우로 판별돼 오른쪽 스와이프가 성립한다. 실제 스크롤 포즈는 거의 수직(위/아래 μ87°)이라 이 축소에 영향받지 않는다 | 사용자 지시 |
| 2026-07-22 | 데모까지 완전 교체: 노트북 `slide_two_fingers_left/right → desktop_switch` 동적 매핑을 capability map에서 제거하고, 시연 탭에서 정적 pose OS 제어를 전면 억제하던 규칙에 **예외**를 둔다 — 노트북 맥락(전구 lock 아님)이고 시연 실행이 켜진 동안 `desktop_prev/next` 정적 스와이프 이벤트만 실행한다. scroll·click 등 나머지 정적 이벤트는 계속 억제해 TCN과 겹치지 않는다. 방향은 정적 스와이프(side−1=오른쪽 도달→next→sink forward=True)가 기존 동적 slide_right→increment와 같은 OS 효과(macOS key 123/←)를 내도록 맞춰 이전 '반대로 매핑' 결정과 체감이 일치한다 | 2026-07-22 '시연 탭은 동적 TCN 하나만 실행'하던 이중실행 방지 결정을 데스크톱 전환에 한해 뒤집는다. 동적 slide→desktop_switch를 제거했으므로 정적 스와이프를 실행해도 이중 실행이 없다. 라이브 관찰에서 좌우 대칭 발화(prev8/next8)로 인식 품질을 확인했고, 실제 데스크톱 전환 방향은 실기기에서 최종 확인 대상(어긋나면 DESKTOP_SWIPE_SIGN 반전) | 사용자 지시 |
| 2026-07-22 | 노트북(컴퓨터) 제어를 **전부 정적 포즈 경로**로 통일한다. capability map에서 laptop의 남은 동적 매핑(slide_up/down→scroll, rotate→volume)까지 제거해 laptop을 빈 `{}`로 만들고, app.py 시연 게이팅을 고쳐 노트북 맥락(전구 lock 아님)+시연 실행 시 desktop_prev/next뿐 아니라 **모든 정적 pose 이벤트**(스크롤·볼륨·커서·클릭·데스크톱)를 실행한다. 동적 TCN 경로는 전구(room.bulb) 전용으로 남는다. slide_up/down·rotate 라벨은 전구에 남아 validate_capability_map 통과 | 실시간 탭에선 이미 스크롤(two_fingers 수직)·볼륨(검지 회전)이 정적이었는데 시연 탭만 노트북을 동적 TCN으로 실행해 경로가 갈렸다. 데스크톱 전환을 정적 스와이프로 옮기며 laptop 동적 매핑을 없앤 김에, 이중 실행 위험도 사라져 시연에서도 노트북을 완전히 정적으로 통일했다(사용자 지시 "스크롤·볼륨도 전부 정적") | 사용자 지시 |
| 2026-07-22 | 손 검출 상한 `num_hands` 2→**1** | 시연 랙 완화 — MediaPipe는 검출된 손마다 landmark 추론을 돌려 손이 둘 잡히면 프레임당 추론이 2회. 1이면 최대 1회라 지연 최소. 트레이드오프: 슬롯 1개라 조작 중 더 큰 새 손으로 바꿔 잡히지 않음(한 손 시연엔 무해) | 사용자 지시 |
| 2026-07-22 | 자세 dwell 재조정: 핀치 클릭류(`pinch_index`/`pinch_middle`) 60→**30ms**, 커서 `index_point` 120→**60ms**(그 외 기본 120·`middle_point` 500 유지) | 클릭·커서 반응성 향상 실험 | 사용자 지시 |
| 2026-07-22 | 중지(`middle_point`)의 '창 닫기'를 **디버깅 툴 창이 활성일 때는 툴 자체 종료**로 가로챔(다른 창 활성이면 평소대로 Cmd/Ctrl+W 전달, `isActiveWindow()`로 판별) | 카메라 앞에서 손만으로 툴을 닫을 수 있게. 모든 close_tab을 가로채면 배경 앱 탭을 못 닫으므로 활성 창으로 대상을 가름 | 사용자 지시 |
| 2026-07-22 | 손가락 **폄 게이트를 거리(`MIN_FINGER_EXTENSION`)→직진도(straightness=MCP→끝 직선거리/관절 세그먼트 합)로 교체 + 검지 커서 게이트 신설**. 검지 `INDEX_STRAIGHTNESS_MIN`=0.97 미만이면 index_point여도 커서 진입 안 함, 두 손가락 스크롤 `TWO_FINGER_STRAIGHTNESS_MIN`=0.85 | 애매하게 굽힌 검지가 index_point로 오분류돼 커서로 새던 문제. 실측(finger_gate_probe: 검지 편[0.994,1.0]/굽힘 μ0.508·중지 굽힘 최대 0.833). 직진도는 비율이라 손 크기·거리·기울기에 불변 — 거리 지표(편 [0.754,1.918]로 기울기에 흔들림)보다 강건 | 사용자 지시 |
| 2026-07-22 | 핀치 **클릭을 마우스 down/up 모델로 전환**(진입=down·릴리즈=up, 클릭↔드래그는 OS 판정 — 유지시간 분기·drag_start/end 이벤트 제거). 더블클릭은 진입 간격 `DOUBLE_CLICK_MS` 600→**800ms** 이내면 2번째 down/up에 `clickState`=2를 실어 macOS가 합침(`InputSink.press`에 `click_state` 인자 추가) | 릴리즈 확정(`RELEASE_FRAMES` ~100ms) 대기로 클릭 딜레이가 있었고, 진입 down이 즉시 반응. macOS는 단일 클릭 두 번만으론 더블클릭 미인식(clickState 필요). 상태기계가 재진입 전 반드시 이탈(up)하므로 up→다음 down 순서 보장 | 사용자 지시 |
| 2026-07-22 | 검지 회전 볼륨에 **워밍업 게이트 `ROT_ACTIVATION_DEG`=270°** 추가: 한 세션 순(net) 회전이 약 3/4바퀴를 넘어야 볼륨 시작(워밍업 중에도 볼륨 노브 락 유지) | 일상 동작의 작은 회전이 갑자기 볼륨을 한두 칸 바꾸던 오작동 방지 — 좌우로 오가는 흔들림은 부호가 상쇄돼 활성화 안 됨. 락을 게이지 채우는 동안에도 걸어 순간 오인식 차단 | 사용자 지시 |
| 2026-07-22 | 정적 분류기 **9-class 확장 — `fist` 유지 + `ok`(엄지-검지 원 → 재생/일시정지) 추가**. 미션 컨트롤은 `fist→open_palm` 전이일 때만(실험 중 '아무 자세/맨손→보 always-fire'로 갔다가 되돌림). 백업 fist(1733)+수집 ok(1715) 병합 재학습 → 정확도 **94.6%**(fist 98.8·ok 100·two_fingers 100) | `ok`로 재생/일시정지 제어. `.claude/tmp/pose_collect.py`(9-class)·`train_pose`로 수집·재학습, 데이터·이전 모델은 `.claude/tmp`에 백업(롤백 가능) | 사용자 지시 |
| 2026-07-22 | 전구(room.bulb)의 **좌우 슬라이드(밝기)도 정적 two_fingers 좌우 스와이프로 이식**한다(사용자 지시 "전구는 좌우 스와이프만"). gaze로 전구가 lock된 동안 정적 스와이프(desktop_prev/next)를 (기기, slide_left/right) `CommitDecision`으로 합성해 동적 경로와 같은 ExecuteWorker로 흘린다(`app._route_bulb_swipe`) — 밝기 델타는 capability map(room.bulb slide_left/right→brightness)에서 그대로 읽혀 config가 단일 진실원. 이중 실행 방지로 동적 slide_left/right가 비노트북 기기에 도달하면 `_handle_commit_decision`에서 건너뛴다. 전구의 up/down(밝기)·rotate(색상)·stop_sign(전원)은 TCN 유지(좌우 스와이프만 이식) | 좌우 슬라이드의 TCN 인식이 불안정해 정적 스와이프(실측 좌우 대칭 발화)가 더 안정적. 전구는 OS 입력이 아니라 adapter 경로라 pose_control로 직접 못 보내므로 결정 합성으로 실행기에 연결. 방향은 왼쪽=감소/오른쪽=증가, 실기기에서 어긋나면 매핑만 뒤집으면 됨 | 사용자 지시 |
| 2026-07-22 | `registration_max_area_radius_deg`을 4.5에서 **6.0으로 되돌린다**(2026-07-22 앞선 결정 취소) | 이 값은 특정 기기 하나가 아니라 등록되는 모든 target에 적용되는 전역 상한이다. 모니터만 좁히려던 의도와 달리, 그 뒤 다시 등록한 전구까지 함께 4.5도로 좁아졌고(실측 확인) 전반적인 시선 인식 체감이 나빠졌다("시선인식이 너무 안 된다"). 실측 중 전구 등록 자체가 사라진 것도 함께 발견해 재등록이 필요하다 | 사용자 지시 |
| 2026-07-22 | 전구 등 다른 기기에서 노트북으로 시선이 **돌아올 때만** OK사인(엄지-검지 원) 확인을 요구하는 게이트를 **Fusion 자신의 Target Lock**(`alignment.py TargetLockTracker`)에 새로 추가한다. `AlignmentConfig.confirmation_gated_targets`(시연은 `{laptop}`)·`confirmation_pre_roll_ms`(기본 300ms)로 구성하고, 외부에서 `note_confirmation_signal(timestamp_ms)`로 신호를 넣는다(app.py `_on_hand`가 `PoseEvent(kind="play_pause")`를 볼 때마다 호출). 처음 확정이거나 같은 target을 유지하는 중에는 걸리지 않는다. OK사인은 계속 재생/일시정지 OS 명령도 그대로 실행한다 — 겹치는 순간이 곧 사용자가 실제로 확인하려는 순간이라 부작용으로 보지 않는다 | Gaze 모듈의 예전 nod gate(`gaze/lock.py`)는 시연 경로(`DemoBridge`→Fusion)에 전혀 연결돼 있지 않아(별도 조사로 확인) 시연에서 아무 효과가 없었다. 사용자가 신뢰하는 확인 동작을 고개 끄덕임→OK사인으로 바꾸고 싶어 했는데, 마침 dev 브랜치에 이미 학습된 `ok`(9-class, 100% 정확도) 포즈가 재생/일시정지 용도로 존재해 재학습 없이 그대로 재사용했다. Fusion의 lock을 Gaze의 lock과 계속 독립적으로 유지하기 위해 Gaze 내부를 참조하지 않고, Fusion 자신의 lock 위에 별도로 게이트를 구현했다 | 사용자 지시 |