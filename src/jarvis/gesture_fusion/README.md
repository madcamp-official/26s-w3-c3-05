# Gesture & Intent Fusion

담당 범위는 `documents/gesture-fusion.md`를 따른다. hand landmark, dynamic spotting,
gesture phase, temporal alignment, fusion score와 Safe Commit을 이 패키지 안에서 구현한다.

입력과 출력은 `jarvis.contracts` 타입을 사용하고, Gaze 및 Runtime의 내부 구현을 직접
참조하지 않는다.

