{
  "_comment": "(target device_id, gesture) -> capability action. jarvis.gesture_fusion.intent.GestureCapabilityMap이 로드한다. capability·operation 이름과 value의 step 제약은 dev-3의 device capability model(documents/runtime-protocol.md, src/jarvis/runtime_protocol/protocol/capability.py)과 맞춰야 한다. 새 제스처·기기를 추가할 때는 코드가 아니라 이 파일만 바꾸면 된다(documents/decisions.md 참고). 실제 매핑은 'devices' 키 아래에 있다 — 이 파일을 파싱할 때 '_'로 시작하는 최상위 키는 무시한다.",
  "devices": {
    "room.bulb": {
      "swipe_down": { "capability": "brightness", "operation": "decrement", "value": 10 },
      "swipe_up": { "capability": "brightness", "operation": "increment", "value": 10 },
      "rotate_clockwise": { "capability": "color_temperature", "operation": "increment", "value": 100 },
      "rotate_counter_clockwise": { "capability": "color_temperature", "operation": "decrement", "value": 100 }
    },
    "laptop": {
      "swipe_down": { "capability": "scroll", "operation": "decrement", "value": 3 },
      "swipe_up": { "capability": "scroll", "operation": "increment", "value": 3 },
      "swipe_left": { "capability": "window_switch", "operation": "decrement", "value": 1 },
      "swipe_right": { "capability": "window_switch", "operation": "increment", "value": 1 },
      "rotate_clockwise": { "capability": "volume", "operation": "increment", "value": 2 },
      "rotate_counter_clockwise": { "capability": "volume", "operation": "decrement", "value": 2 }
    }
  }
}
